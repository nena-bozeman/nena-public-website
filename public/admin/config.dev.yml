# Local CMS backend — merged with config.base.yml via pnpm cms:use-dev-config.
# Requires decap-server on port 8081 (see .devcontainer/start.sh).

local_backend: true
backend:
  name: github
  repo: nena-bozeman/nena-public-website
  branch: main
