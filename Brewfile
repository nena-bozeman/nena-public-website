# Homebrew dependencies for local development (not covered by pnpm).
# Install from repo root: brew bundle
# Check only:            brew bundle check
# See docs/link-checking.md

# Pin: keep in sync with .lychee-version (CI reads the same file).
brew "lychee"
